apiVersion: v1
kind: Service
metadata:
  name: weather-service
spec:
  type: LoadBalancer  # 외부에서 접속 가능하게 설정
  selector:
    app: weather-app
  ports:
    - protocol: TCP
      port: 8082      # 외부 접속 포트를 8082로 변경 (권한 문제 방지)
      targetPort: 8000  # 내부 컨테이너의 8000번 포트로 연결
